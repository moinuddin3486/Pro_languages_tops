#include <stdio.h>
int main()
{
    int number;
    printf("Enter your number:");
    scanf("%d", &number);

    // true conditinon pela lakvani
    (number % 2 == 0) ? printf("Even") : printf("Odd");
} // beki          // eki
